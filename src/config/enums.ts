export enum envEnums {
  DATABASE_URL = 'DATABASE_URL',
  PORT = 'PORT',
  ENVIRONMENT = 'ENVIRONMENT',
}

export enum Environments {
  local = 'local',
  dev = 'dev',
  stage = 'stage',
  prod = 'prod',
}
